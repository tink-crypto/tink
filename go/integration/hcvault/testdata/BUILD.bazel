package(default_visibility = ["//:__subpackages__"])

licenses(["notice"])  # keep

filegroup(
    name = "server_tls_files",
    testonly = 1,
    srcs = [
        "server.crt",
        "server.key",
    ],
)
